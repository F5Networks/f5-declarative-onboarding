image: node:8-alpine

stages:
    - test
    - package

test:
    stage: test
    tags:
        - docker-executor
    script:
        - npm install
        - npm test

build_rpm:
    stage: package
    tags:
        - docker-executor
    only:
        - /^release-.*/
    script:
        - apk add rpm bash
        - build/buildRpm.sh
